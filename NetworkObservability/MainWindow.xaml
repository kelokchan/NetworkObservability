<Window x:Class="NetworkObservability.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetworkObservability"
        mc:Ignorable="d"
        WindowState="Normal"
        Title="MainWindow"  AllowDrop="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Menu Padding="5 0 5 0">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open"/>
                    <MenuItem Header="_Close"/>
                    <MenuItem Header="_Save"/>
                </MenuItem>

                <MenuItem Header="_Add">
                    <MenuItem Header="_Node"/>
                    <MenuItem Header="_Arc"/>
                </MenuItem>
            </Menu>
            <ToolBar>
                <Button Margin="20 0 0 0" Content="Start" Width="50" HorizontalAlignment="Left" Style="{DynamicResource ButtonStyleForStart}"/>
            </ToolBar>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GridSplitter HorizontalAlignment="Right" 
                  VerticalAlignment="Stretch" 
                  Grid.Column="1" ResizeBehavior="PreviousAndNext"
                  Width="5" Background="#FFBCBCBC"/>
            <StackPanel Grid.Column="2">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Height="Auto">

                        <!-- Right panel -->
                        <Label Content="Right" />
                        <!-- Right panel -->

                    </Grid>
                </ScrollViewer>
            </StackPanel>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Visible" 
                              VerticalScrollBarVisibility="Visible">
                    <!-- Canvas -->
                    <Canvas Name="canvas" Background="AliceBlue" Width="3500" Height="1200" AllowDrop="True" Drop="Canvas_Drop">
                    </Canvas>
                    <!-- Canvas -->
                </ScrollViewer>
                <GridSplitter VerticalAlignment="Center" Grid.Row="1" 
                              Height="5" Background="#FFBCBCBC" HorizontalAlignment="Stretch"/>
                <!--<Label Content="Bottom Panel" Grid.Row="2" /> -->
                <Border Grid.Row="2" Margin="1 0 0 0">
                    <TabControl TabStripPlacement="Bottom" BorderThickness="1,0,1,1" >
                        <TabItem Header="Components">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <!--<UniformGrid Rows="3" Columns="1" HorizontalAlignment="Left" Height="Auto">
                                    <Grid Name="Blob">
                                        <Ellipse Fill="Black" Height="30" Stroke="Black" Width="30"  HorizontalAlignment="Center" Margin="30 10 30 10"/>
                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Bottom" Text="Blob" Margin="0,50,0,0"/>
                                    </Grid>
                                </UniformGrid>-->
                                <Label Content="Test Drag" MouseDown="Label_MouseDown"/>
                                <!--<ItemsControl ItemsSource="{Binding}" />-->
                            </ScrollViewer>
                        </TabItem>
                        <!-- <TabItem Header="Description"></TabItem> -->
                        <TabItem Header="Log">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <Grid Height="Auto">
                                    <!-- Log panel -->
                                    <Label Content="> See log messages here..." />
                                    <!-- Code here -->
                                    <!-- Log panel -->

                                </Grid>
                            </ScrollViewer>
                        </TabItem>
                    </TabControl>
                </Border>
            </Grid>
        </Grid>
    </Grid>

</Window>
