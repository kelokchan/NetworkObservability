<Window 
        x:Class="NetworkObservability.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:custom="clr-namespace:NetworkObservability"
        mc:Ignorable="d"
        WindowState="Normal"
        Title="MainWindow"  AllowDrop="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Menu Padding="5 0 5 0">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open"/>
                    <MenuItem Header="_Close"/>
                    <MenuItem Header="_Save"/>
                </MenuItem>

                <MenuItem Header="_Add">
                    <MenuItem Header="_Node"/>
                    <MenuItem Header="_Arc"/>
                </MenuItem>
            </Menu>
            <ToolBar>
                <Button Margin="20 0 0 0" Content="Start" Width="50" HorizontalAlignment="Left" Style="{DynamicResource ButtonStyleForStart}"/>
            </ToolBar>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GridSplitter HorizontalAlignment="Right" 
                  VerticalAlignment="Stretch" 
                  Grid.Column="1" ResizeBehavior="PreviousAndNext"
                  Width="5" Background="#FFBCBCBC"/>
            <StackPanel Grid.Column="2">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <!-- Right panel -->
                        <Label Content="Properties: " BorderThickness="0,0,0,1" BorderBrush="Gray" />
                        <!-- Right panel -->
                </ScrollViewer>
                <!--<ListView Height="100" Name="nodePropertiesListView">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Name: " FontWeight="Bold"></TextBlock>
                                <TextBox Text="{Binding x}"></TextBox>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>-->
                <Grid Name="nodePropertiesPanel" Margin="2 5 2 2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="ID: "/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0 0 0 5" Text="{Binding ID, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Label: "/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0 0 0 5" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                </Grid>
            </StackPanel>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Visible" 
                              VerticalScrollBarVisibility="Visible">
                    <!-- Canvas -->
                    <custom:DragCanvas x:Name="canvas" Background="AliceBlue" Width="3500" Height="1200" AllowDrop="True" AllowDragging="True" PreviewMouseRightButtonDown="canvas_PreviewMouseRightButtonDown" Drop="canvas_Drop">
                        <custom:DragCanvas.ContextMenu>
                            <ContextMenu Opened="OnContextMenuOpened"  MenuItem.Click="OnMenuItemClick">
                                <MenuItem Header="Bring To Front" Name="menuItemBringToFront" />
                                <MenuItem Header="Send To Back" Name="menuItemSendToBack" />
                            </ContextMenu>
                        </custom:DragCanvas.ContextMenu>
                    </custom:DragCanvas>
                    <!-- Canvas -->
                </ScrollViewer>
                <GridSplitter VerticalAlignment="Center" Grid.Row="1" 
                              Height="5" Background="#FFBCBCBC" HorizontalAlignment="Stretch"/>
                <!--<Label Content="Bottom Panel" Grid.Row="2" /> -->
                <Border Grid.Row="2" Margin="1 0 0 0">
                    <TabControl TabStripPlacement="Bottom" BorderThickness="1,0,1,1" >
                        <TabItem Header="Components">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <Image Height="50" Width="50" Source="Images/circle.gif" PreviewMouseDown="Image_PreviewMouseDown" VerticalAlignment="Top" Margin="0,20,0,0"/>
                            </ScrollViewer>
                        </TabItem>
                        <!-- <TabItem Header="Description"></TabItem> -->
                        <TabItem Header="Log">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <Grid Height="Auto">
                                    <!-- Log panel -->
                                    <Label Content="> See log messages here..." />
                                    <!-- Code here -->
                                    <!-- Log panel -->

                                </Grid>
                            </ScrollViewer>
                        </TabItem>
                    </TabControl>
                </Border>
            </Grid>
        </Grid>
    </Grid>

</Window>
